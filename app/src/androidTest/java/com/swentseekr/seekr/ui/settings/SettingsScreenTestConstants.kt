package com.swentseekr.seekr.ui.settings

object SettingsScreenTestConstants {
  const val REPETITION_TIMES = 3
  const val PERMISSION_EVENTS = "_permissionEvents"
  const val UNCHECKED_CAST = "UNCHECKED_CAST"
  const val VIEW_MODEL_MESSAGE = "ViewModel should still be valid after permission flow"
  const val INITIAL_STATE_MESSAGE = "Initial state should not be null"
  const val NOTIFICATION_MESSAGE = "Notifications should remain enabled after multiple grants"
  const val NOTIFICATION_ENABLED = "Notifications should be enabled after granting"
  const val PICTURES_MESSAGE = "Pictures should be enabled after granting"
  const val LOCALISATION_MESSAGE = "Localisation should be enabled after granting"
  const val UI_STATE = "_uiState"
}
